name: Keep Render service awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check URL
        run: curl -sS -o /dev/null -w "HTTP %{http_code}\n" https://zukas-guide-bot-2026.onrender.com
