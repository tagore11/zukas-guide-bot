services:
  - type: web
    name: zukas-guide-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python kas_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
